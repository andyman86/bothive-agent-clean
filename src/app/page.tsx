"use client";
import { useState } from "react";
import { agentConfig } from "./agent.config"; // << correct path + named import

export default function Home() {
  const [open, setOpen] = useState(false);
  // ...your code...
}
